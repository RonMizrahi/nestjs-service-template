# Base URL of the NestJS service the SPA calls. Defaults to http://localhost:3000.
# The service's CORS_ORIGINS defaults to '*', which works with the Bearer-token flow;
# tighten it to http://localhost:5173 for a stricter local setup.
VITE_API_BASE_URL=http://localhost:3000
